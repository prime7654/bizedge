# Django 5.2 is the current LTS. Note: CheckConstraint(condition=...) used in
# apps/grievances/models.py requires Django >= 5.1 -- do not downgrade below it
# without rewriting those constraints to the older check= keyword.
Django==5.2.17
djangorestframework==3.18.0
psycopg[binary]==3.2.3
django-environ==0.14.0
drf-spectacular==0.30.0
django-filter==26.1
celery==5.6.3
redis==8.1.0
gunicorn==23.0.0
whitenoise==6.8.2
